load 'deploy' if respond_to?(:namespace) # cap2 differentiator

# taken from old Capfile. Still needed? 
# Dir['vendor/plugins/*/recipes/*.rb'].each { |plugin| load(plugin) }

# basics
load 'config/deploy/settings'
load 'config/deploy/symlinks'

load 'config/deploy'

